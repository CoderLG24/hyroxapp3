# Plan View Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Redesign the Plan tab into a focus-date-centered timeline with inline expandable workout details and stronger emphasis on nearby dates.

**Architecture:** Keep `focusDate` in the shared app store and add plan-local UI state for expanded week/day behavior. Extract plan-specific components for the sticky context bar, week cards, and inline workout detail panels so the route stays maintainable and the interaction stays contained to the Plan section.

**Tech Stack:** Next.js App Router, TypeScript, React, Tailwind CSS, Framer Motion

---

### Task 1: Add Plan Interaction Tests

**Files:**
- Create: `tests/app/plan-page.test.tsx`
- Modify: `vitest.setup.ts`

**Step 1: Write the failing test**

Add tests that assert:

- the focused week is expanded by default
- clicking a day shows inline workout details
- a jump-to-current-week control appears

**Step 2: Run test to verify it fails**

Run: `npm run test -- tests/app/plan-page.test.tsx`
Expected: FAIL because the current Plan route does not support inline expansion behavior

**Step 3: Write minimal implementation support**

Add any small test setup needed to render the Plan route with the app store.

**Step 4: Run test to verify it still fails for the right reason**

Run: `npm run test -- tests/app/plan-page.test.tsx`
Expected: FAIL due to missing Plan interactions rather than missing test infrastructure

### Task 2: Extract Plan Components

**Files:**
- Create: `components/plan/plan-context-bar.tsx`
- Create: `components/plan/plan-week-card.tsx`
- Create: `components/plan/plan-day-card.tsx`
- Create: `components/plan/plan-workout-inline.tsx`
- Modify: `app/plan/page.tsx`

**Step 1: Implement the focused timeline layout**

Add:

- sticky context bar
- centered nearby-week stack
- compressed distant-week presentation

**Step 2: Implement inline day expansion**

Make clicking a day card expand workout details directly in the Plan screen.

**Step 3: Add jump-to-current-week behavior**

Use the current `focusDate` to return the user to the relevant week block.

**Step 4: Run the targeted test**

Run: `npm run test -- tests/app/plan-page.test.tsx`
Expected: PASS

### Task 3: Refine Plan State And Visual Hierarchy

**Files:**
- Modify: `app/plan/page.tsx`
- Modify: `components/plan/plan-week-card.tsx`
- Modify: `components/plan/plan-day-card.tsx`

**Step 1: Improve emphasis on nearby dates**

Ensure:

- current week is visually dominant
- adjacent weeks are easy to access
- far weeks are compact but still browsable

**Step 2: Keep details fully contained in Plan**

Ensure the user does not need to leave Plan to understand the workout.

**Step 3: Run lint and targeted tests**

Run: `npm run test -- tests/app/plan-page.test.tsx`
Expected: PASS

Run: `npm run lint`
Expected: PASS

### Task 4: Final Verification

**Files:**
- None required

**Step 1: Run full test suite**

Run: `npm run test`
Expected: PASS

**Step 2: Run production build**

Run: `npm run build`
Expected: PASS

**Step 3: Review Plan route behavior**

Confirm the Plan section:

- anchors around the relevant date
- expands workouts inline
- makes close dates feel primary
- keeps distant dates secondary
