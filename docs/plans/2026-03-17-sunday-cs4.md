# Sunday CS4 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make every Sunday in the seed range an explicit `Hyrox CS4 Class` workout for both athletes.

**Architecture:** Add a reusable Sunday class workout template and apply it across the workout generator for both athletes. Update the seed-coverage tests so they assert the new Sunday rule instead of the previous Sunday rest behavior.

**Tech Stack:** TypeScript, Next.js seed modules, Vitest

---

### Task 1: Update Seed Tests For Sunday Rules

**Files:**
- Modify: `tests/data/workout-seeds.test.ts`

**Step 1: Write the failing test**

Replace the Sunday rest expectation with assertions that all Sundays are `Hyrox CS4 Class` for both athletes.

**Step 2: Run test to verify it fails**

Run: `npm run test -- tests/data/workout-seeds.test.ts`
Expected: FAIL because Sundays are currently still generated as rest days

### Task 2: Add Sunday CS4 Workout Template

**Files:**
- Modify: `lib/workout-builders.ts`
- Modify: `lib/training-plan-source.ts`

**Step 1: Implement an explicit Sunday class template**

Add a reusable `Hyrox CS4 Class` workout object builder with exact workout content.

**Step 2: Apply it to all Sundays**

Replace all Sunday rest templates in the generator with the new Sunday class template for both athletes.

**Step 3: Run targeted seed tests**

Run: `npm run test -- tests/data/workout-seeds.test.ts`
Expected: PASS

### Task 3: Final Verification

**Files:**
- None required

**Step 1: Run full test suite**

Run: `npm run test`
Expected: PASS

**Step 2: Run lint**

Run: `npm run lint`
Expected: PASS

**Step 3: Run production build**

Run: `npm run build`
Expected: PASS
